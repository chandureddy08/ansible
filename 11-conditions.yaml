- name: conditions
  hosts: web
  become: yes
  tasks:
  - name: check user is existing or not
    ansible.builtin.command: id expense

  - name: say hello
    ansible.builtin.debug:
      msg: "Hello"