- name: install and run nginx
  become: yes
  hosts: web
  tasks:
   - name: install nginx
     ansible.builtin.dnf:
      name: nginx
      state: latest
   - name: run nginx
     ansible.buitlin.service:
      name: nginx
      state: started
      enabled: yes

    